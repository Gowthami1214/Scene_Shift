# ==============================================================================
# SceneShift — Requirements
# Python 3.10+  |  CUDA 12.1 (GPU optional, CPU fallback included)
# ==============================================================================

# ── Core ML / Deep Learning ───────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python>=4.8.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0

# ── Scientific Computing ──────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0

# ── AI Models ─────────────────────────────────────────────────────────────────
# Hugging Face ecosystem
diffusers>=0.24.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.1
huggingface-hub>=0.20.0
tokenizers>=0.15.0
sentencepiece>=0.1.99

# YOLOv8 (Ultralytics)
ultralytics>=8.0.200

# SAM2 — install from GitHub:
#   pip install git+https://github.com/facebookresearch/sam2.git
# Listed here for reference:
# sam2

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# ── Frontend / UI ─────────────────────────────────────────────────────────────
gradio>=4.7.1

# ── Async / Task Queue ────────────────────────────────────────────────────────
anyio>=4.1.0
httpx>=0.25.0

# ── Storage / Caching ─────────────────────────────────────────────────────────
redis>=5.0.1         # Optional: Celery backend
celery>=5.3.4        # Optional: distributed task queue

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
loguru>=0.7.2
tqdm>=4.66.0
rich>=13.7.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
httpx>=0.25.0           # For FastAPI TestClient

# ── Dev Tools ─────────────────────────────────────────────────────────────────
black>=23.11.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.7.0
